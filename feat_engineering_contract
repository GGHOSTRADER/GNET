Feature Engineering Contract

*COMMENT: MUST INCLUDE CONTENT OF PRE MADE FUNCTIONS AND CLASSES


Global (Done once before any feature calculations):
Steps
1) Connect to redis using get_redis_connection function from newtwo_files.redis_tool.py
2) xread from redis using parse_xread_to_bars , XReadBatch ,XReadShapeError from bar_codec.py
3) Parse, create container, cast type with bar_from_redis_fields ,_as_str , DecodeError from bar_codec.py

Live tests 
1) len(xread Redis data) = 11 

2) Data:
2.1) Symbol:
Ticker Symbol
Value Type: String
Calculation = None
Invariants = symbol != null , symbol type == string

2.2) Date: 
Date in YYMMDD
Value type = Interger
Calculation = Ts uses a propietary interger version which is 1900 the starting point YYYMMDD where YYY = years since 1900
Example =  April 12 of 1999 = 990412
Invariants =  date > 1260000 , 12 >= MM >= 1 , 31 >= DD >= 1,

2.3) Time in Seconds:
Extracting current time of the day since midnight, comes in seconds
Value type = Interger
Calculation =
HOUR 0 == Midnight
HOUR * 3600 = X
MINUTE * 60 = Y
SECOND * 1 = Z
TOTAL SECONDS = X + Y + Z

2.4) Open:
Price at which this bar started trading
Value type = Double
Calculation = None
Invariant = Open > 0 , Open isinstance float

2.5) High:
Highest Price traded in the trading interval
Value type = Double
Calculation = None
Invariant = Open <= High & High != 0 , High type isinstance float

2.6) Low:
Low Price traded in the trading interval
Value type = Double
Calculation = None
Invariant = Open >= Low & Low != 0, Low type isinstance float

2.7) Close:
Last price when the bar interval stopped trading
Value type = Double
Calculation = None
Invariant = Close >= Low & Close <= High & Close != 0 , Close type isinstance float

2.8) Up:
Meaning = Market Buy orders
Value type = Interger
Calculation = None
Invariant = Up >= 0 , Up type isinstance int

2.9) Down:
Meaning = Market Sell orders
Value type = Interger
Calculation = None
Invariant = Down >= 0 , Down isinstance int

2.10) Vwap:
Meaning = Volume Weighted Average Price since market Open
Value type = Double
Components = Volume: Sum of transactions (Up + Down), High: Highest price traded in the interval, Low: Lowest traded price in the interval, Close: Last traded price in the interval.	
Calculation = Sum(((high+low+close)/3)*(Up+Down)) / Sum(Up+Down)   {Not calculated here}
Invariant = vwap >= 0 , Vwap isinstance Float

2.11) Bar Number:
Meaning = Bar number Since the start of bar
Value type = Interger
Calculation = None
Invariant = bar_number[x] > bar_number[x-1] , barnumber != -n , Bar Number type isinstance int


3) Bar Continuality : bar_num[-1] - bar_num[-2] == 1


Internal(per Feature)

modSlope5
----
Input :  5 bar window Object called b, b class that contains the attributes .low , .high .close , .bar_num
Input Origin: comes from global process
Style: Pure Function , Clean architecture
Data type : Float 
Description : Slope of the last 5 bars
Calculation : (high[-1] + low[-1] + close[-1])/3 - (high[-5] + low[-5] + close[-5])/3
invariants : 
	bar value = math.isfinite(modSlope5) # To cover Nan and inf
	end points difference = bar_num[-1] - bar_num[-5] == 4,  
	Buffer = len("buffer name") => 5 , 
	Bar continuinity = all(bar_nums[i] == bar_nums[i-1] + 1 for i in range(1, 5))


