# Pending Work

## 1. Implementation and Changes from the Last Session

1. Added a canonical MA feature module shared by live feature generation and offline training, eliminating duplicated feature formulas.
2. Removed the obsolete duplicate MA feature implementations after confirming they were no longer imported.
3. Added the strategy-candidate contract and codec with validation for strategy ID, candidate ID, symbol, bar identity, and direction.
4. Added `candidate_tcp_server.py` and the shared `trade_candidates` Redis stream on TCP port 9012.
5. Added `StrategyBridge.dll` source so all TradeStation strategy windows can send candidates through one shared TCP channel.
6. Added `strategy_router.py`, which:
   - loads the MA2CrossLE model and scaler once;
   - accepts features and candidates in either arrival order;
   - matches only exact `symbol + date + time_s` records and retains TradeStation's study-local `bar_num` for diagnostics;
   - rejects missing features, unknown strategies, duplicates, and inference errors explicitly;
   - publishes exact-candidate results to `trade_decisions`.
7. Changed the live path from inference on every bar to inference only when a primary strategy candidate arrives.
8. Updated `signal_tcp_server.py` to return exact-candidate decisions rather than the old generic `trade_signal` payload.
9. Updated `SignalBridge.dll` with independent decision queues and candidate-ID retrieval; queues now use strategy-instance identity.
10. Compiled and verified 32-bit `StrategyBridge.dll` and `SignalBridge.dll` exports for TradeStation.
11. Reworked `launch.ps1` to:
    - wait for Redis `PING`;
    - reject occupied GNET ports;
    - start consumers before producers;
    - wait for router/model readiness;
    - verify TCP listeners without connecting to them;
    - launch TradeStation last.
12. Updated the Mermaid diagrams, README, launch guide, Redis reference, DLL instructions, EasyLanguage integration guidance, training paths, project control center, `AGENTS.md`, and `CLAUDE.md` for the candidate-router architecture.
13. Added router, protocol, and model-registry tests.
14. Added `instance_id` across the candidate and decision protocols, moved GUID candidate-ID generation into `StrategyBridge.dll`, and changed decision queues to route by strategy instance.
15. Anchored the decision server's Redis cursor at service startup and preserved it across DLL reconnects so a decision cannot be lost before the first TradeStation poll.
16. Replaced the router's hard-coded MA model mapping with a validated directory registry and added a local browser page for enabling models and configuring threshold/device.
17. Added the registry UI to `launch.ps1`, made its readiness check HTTP-based, opened it automatically, and allowed the launcher to reuse an already-running local UI.
18. Added `EL_files/MA2CrossLE_GNET.els`, a GNET-enabled copy of the TradeStation MA crossover strategy that sends one uniquely identified candidate, polls by strategy instance, verifies the returned GUID, and submits the long order only after approval.
19. The GNET-enabled MA strategy was imported and compiled successfully in TradeStation.
20. Added exact-candidate latency instrumentation from Python candidate receipt through feature matching, inference, Redis decision publication, and TCP delivery to `SignalBridge.dll`.
21. Added an opt-in Windows Scheduled Task and watchdog with explicit ON/OFF scripts; the watchdog launches GNET and restarts failed Python services while enabled.
22. Added durable Redis consumer groups: the router acknowledges candidates after decision publication, and the signal server acknowledges decisions after TCP delivery.
23. Audited and removed the unused `feat_eng_1.py` prototype and its isolated test. `consolidator.py` remains an optional print-only debugger and does not currently feed inference.
24. Installed the `GNET Pipeline Watchdog` Windows Scheduled Task successfully in its initial OFF state and documented the daily ON/OFF/status commands.
25. Changed the live VP session boundary from a calendar-date reset to the ES 18:00 session open; the profile now remains continuous across midnight and preserves each snapshot's calendar date for exact feature matching.
26. Added `training_mlp.prepare_historical_vp`, which validates the four TradeStation tick exports, converts them to Zstandard-compressed Parquet by 18:00 session, resolves identical overlapping sessions, rejects conflicting overlaps by default, and writes JSON/CSV manifests.
27. Moved the four large TradeStation exports out of the repository root into `historical_vp/raw/`; generated Parquet and manifests now default to `historical_vp/prepared/`.
28. Made historical preparation resumable: conflicting overlaps preserve compressed staging data and can be finalized with `--resume` without scanning or converting the 5 GB raw exports again.
29. Expanded `historical_vp/README.md` into a complete setup and operations guide and added a dedicated Mermaid diagram for validation, session partitioning, overlap handling, staging, and finalization.
30. Changed historical VP preparation to discover every numbered `volumeprof<number>.txt` export automatically and process the files in numeric order.
31. Optimized historical VP preparation with strict fixed-format date/time parsing, configurable process-parallel source staging (two workers by default), and throughput metrics in the completion summary.
32. Extracted canonical, transport-independent volume-profile state and mathematics into `feat_files/canonical_volume_profile.py`; the live Redis service now imports that engine through compatibility aliases, ready for historical Parquet replay to use the same code.
33. Expanded the canonical VP contract to 32 candidate features using TradeStation-classified Up/Down volume, including cumulative and price-level delta, recent divergence, Value Area dynamics, profile shape, acceptance, POC velocity, and grouped HVN/LVN measurements; scalar fields now publish live through Redis.
34. Added PID-scoped `stop.ps1` and `restart.ps1` commands; `launch.ps1` now records every managed service terminal, shutdown disables the watchdog and verifies GNET ports, and Redis data is preserved by default.
35. Expanded the workflow guide with normal and legacy shutdown behavior, watchdog elevation troubleshooting, and an explicit matrix of what happens to Docker Desktop, Redis, TradeStation, and stored stream data.
36. Added guarded `nuke.ps1` for an intentional hard reset: it requires explicit data-loss confirmation, verifies the exact `redis1` target, removes all Redis state, optionally recreates an empty container, and then gracefully shuts down Docker Desktop and its engine without deleting unrelated containers.
37. Added `kill_tradestation.ps1` with list-only inspection, path/company-scoped force termination, and post-kill verification for recovering from a frozen TradeStation instance.
38. Updated the setup workflow with the exact saved TradeStation document names: `g_cpp_dll_bar`, `g_cpp_dll_tick`, and `G_MA_CROSS_NN`, while preserving their source-file and DLL mappings.
39. Added same-named EasyLanguage backups under `EL_files/` for the installed TradeStation strategy and bar/tick indicators, plus a backup mapping README and direct setup-guide links.
40. Added prominent startup-order warnings to the main README and live workflow guide: all four Python TCP endpoints must be listening before enabling the TradeStation components, or DLL reconnection attempts can leave TradeStation stuck in a waiting loop.
41. Hardened all four TradeStation DLL sources with a shared fail-fast socket layer: 2 ms bounded non-blocking connection attempts, two-second failure backoff, non-blocking established sockets, and try-locks that return immediately rather than waiting on a chart thread. Added a coordinated x86 staging build script.
42. Hardened the three installed EasyLanguage backups with live-bar-only DLL traffic and transition-only connection logging; the MA strategy also times out lost pending candidates while keeping its protective exits independent of ML connectivity.
43. Added `check_gnet_ports.ps1`, a readiness check and continuously refreshing `-Watch` dashboard for ports 9009–9012 that defaults to a ten-second interval, shows the mapped service and owning process, warns against enabling TradeStation when any endpoint is missing, and retains a scriptable one-shot success/failure mode.
44. Added the live port monitor to the scoped shutdown allowlist, so `stop.ps1`, `restart.ps1`, and `nuke.ps1` also close PowerShell processes running `check_gnet_ports.ps1 -Watch` without targeting unrelated terminals.
45. Added non-destructive `stop.ps1 -StopDockerDesktop`: it stops GNET and `redis1`, preserves Redis data, and gracefully closes Docker Desktop and its engine to release memory.
46. Fixed the one-client lifetime bug in the bar and tick TCP ingestion servers. Previously, a chart/workspace/TradeStation disconnect ended the only `recv()` loop, closed ports 9009/9010, and forced a manual restart even when the terminal still showed stale output. Both processes now keep their listening socket alive, return to `accept()` after graceful disconnects or resets, isolate byte buffers per client, intentionally discard incomplete trailing records, and have regression tests covering replacement connections and cross-session contamination. Existing terminals 7 and 8 must be restarted once to load this fix; later DLL reconnects do not require another restart.
47. Added and executed `training_mlp.replay_historical_vp`. It replays every prepared tick through the canonical VP engine on four worker processes, retains the freshest final-second state, commits temporal history once per 30-second interval, and writes one Zstandard Parquet per session plus JSON/CSV manifests. The completed dataset contains 115,879,384 ticks, 184,120 unique feature rows, 110 sessions, 51 output columns, and all 32 finite canonical VP features in 35,361,522 bytes (about 35 MB). This reduces roughly 6 GB of raw tick exports to about 35 MB because the output stores one derived feature row per 30-second interval instead of every tick, followed by Zstandard compression; it is a feature-dataset reduction, not a lossless 6 GB-to-35 MB compression of the raw ticks.
48. Added and executed `training_mlp.analyze_vp_correlations` across all 184,120 historical feature rows. It writes complete Pearson and Spearman matrices, all 496 unique feature pairs, threshold-filtered pairs, and feature diagnostics under `historical_vp/features/correlation/`. With high redundancy defined as `|r| >= 0.90` and severe redundancy as `|r| >= 0.95`, one pair was severe (`classified_delta_ratio` versus `classified_value_area_delta_ratio`, Pearson 0.9744) and two additional pairs were high by Spearman (`poc_concentration` versus `profile_entropy`, -0.9485; `va_width` versus `profile_entropy`, 0.9390). No feature was constant. No feature was removed automatically; final selection must be validated against out-of-sample model performance.
49. Added a canonical event-aware financial-validation package under `training_mlp/validation/`. `PurgedWalkForward` provides past-only expanding folds with label-interval purging; `CombinatorialPurgedCrossValidation` provides CPCV(N, k), observation-count embargoes, and deterministic backtest-path assignments; both share one tested `[event_start, t1]` overlap implementation. The modules remain separate and importable so MA, Zona, and future VP trainers do not duplicate split logic.
50. Migrated MA labeling and training to actual event-end timestamps. `study_pipeline.py` now pairs each numbered TradeStation entry with its following close, interprets TradeStation date-only timestamps as midnight, excludes the explicit open trade, validates completed pairs, and writes `t1` without adding exit type as a feature. The regenerated MA dataset contains 17,768 completed trades, 8,835 positive labels, 8,933 negative labels, and valid `t1` on every row. `30_training_mlp.py` now imports the canonical purged holdout and expanding walk-forward modules; its duplicated fixed-row splitters were removed and timestamps remain outside the 13-feature MLP contract.
51. Confirmed the current wiring boundary. The MA training path is wired to the `t1`-aware dataset and canonical purged walk-forward validation, but the 32 VP features remain isolated from MA training, the MA model input contract, `strategy_router.py`, and live inference. Historical VP Parquets currently feed only offline replay/correlation analysis, while the live VP stream feeds only the optional print-only `consolidator.py`. CPCV is implemented, exported, and tested, but no training or evaluation runner instantiates it yet.
52. Aligned the live MA scientific runtime with the serialized artifact: installed and pinned `scikit-learn==1.8.0`, `numpy==1.26.4`, and `scipy==1.13.1`. Verified clean imports for NumPy, SciPy, scikit-learn, pandas, PyArrow, NumExpr, and Bottleneck, then loaded the enabled `MA2CrossLE` scaler and PyTorch model together on CPU with the canonical 13-feature contract and no serialization-version warning.
53. Added an always-on candidate/decision integration test using real loopback TCP sockets at both TradeStation boundaries and an in-memory Redis stream boundary. It verifies candidate TCP ingestion, Redis-shaped encoding, exact router matching, decision publication, exact-candidate port-9011 payload delivery, and candidate/decision acknowledgements. Docker-backed Redis consumer-group recovery, malformed-stream, and restart tests remain pending because Docker was off during this run.
54. Profiled canonical VP costs on real low-, median-, and high-activity historical sessions with `training_mlp.profile_vp_cost`. Incremental updates cost roughly 2 microseconds per tick and full snapshots roughly 0.16–0.27 ms each. The highest-activity session had a median of 10, p95 of 117, p99 of 185, and maximum of 4,158 ticks in a final second. Current every-final-tick snapshots therefore ranged from about 2.6 ms at the median to roughly 1.1 seconds in the largest observed burst, while three previews plus one history advancement remained around 1 ms. Results are stored under `historical_vp/features/profile/` and documented in `documentation/vp_cost_profile.md`.
55. Added a passive live tick-pipeline profiler. Tick ingestion now records when a complete TCP line is available, validation records when processing starts, and Redis stream IDs provide the raw, validated, and VP publication times. `python -m netwo_files.tick_pipeline_profiler` joins only exact tick identities and refreshes p50/p95/p99/max latency for every observable hop without changing the canonical Tick or VP math.
56. Fixed the router's Redis consumer-group startup transition. Redis may return `[[trade_candidates, []]]` when the consumer has no pending entries; the router previously treated that truthy wrapper as work and remained on ID `0-0` forever instead of switching to `>`, leaving every new candidate unread. The router now checks for actual inner entries, matching the already-hardened decision server behavior. Candidate, router, and decision-delivery logs now consistently include local ISO time, TradeStation date/time, and the exact candidate GUID.
57. Added `launch_grid.ps1`, which preserves readiness ordering while placing all nine services in one named Windows Terminal window: Strategy, Tick Pipeline, and Bar and Operations tabs each contain three independent panes. `restart.ps1` and full watchdog recovery now use the grid; an individually recovered service opens a labeled recovery tab in the same GNET window. The legacy nine-window `launch.ps1` remains available.
58. Hardened grid-pane startup after the first live smoke test exposed Windows Terminal re-parsing semicolons in inline PowerShell commands and attempting to launch `$host.UI...` as an executable. Every pane and watchdog recovery now calls the validated `automation/run_service_terminal.ps1` entry point with a simple allowlisted service key, eliminating inline-command quoting. Scoped shutdown also recognizes these exact pane hosts.
59. Made `stop.ps1 -StopTradeStation` enforce a completed TradeStation shutdown. It sends `CloseMainWindow`, waits up to ten seconds, then force-closes only processes verified by TradeStation installation path or company metadata and confirms none remain. This makes `.\stop.ps1 -StopTradeStation -StopDockerDesktop` a true one-command full shutdown while preserving Redis data; unsaved TradeStation workspace changes can be lost after the grace period.
60. Made Redis/Docker shutdown idempotent. If the Docker engine and Desktop processes are already unavailable, `stop.ps1 -StopDockerDesktop` now reports Redis and Docker as already stopped instead of throwing because `docker stop redis1` cannot connect to a nonexistent engine. A reachable Docker engine that genuinely fails to stop `redis1` still raises an error.

## 2. TODO / Next Steps

1. Validate the compiled strategy's DLL calls and intrabar decision polling at runtime in TradeStation; compilation is complete, but runtime behavior still needs the live pipeline.
2. Run a full live end-to-end test with TradeStation, all eight Python services, Redis, and the four DLLs. Unit tests do not yet validate the complete external system.
3. Test several TradeStation strategy windows firing simultaneously and measure candidate-to-decision latency, ordering, and cross-strategy isolation.
4. Replace the router's current in-process sequential inference with independent per-strategy workers if simultaneous-strategy benchmarks show material queueing. The current router is race-safe but inference calls are still sequential within one Python process.
5. Add and validate a Zona registry directory once its production artifact set and live feature contract are finalized.
6. Run the new live tick-pipeline profiler during active market data and use its measured p95/p99 arrival and queueing latency to select the VP interval policy. Do not introduce an unbounded wait for the next tick: compare an immediate boundary snapshot with a strictly bounded late-arrival allowance, then align the live adapter with the offline one-commit-per-interval semantics.
7. Turn the current print-only `consolidator.py` concept into a production feature join only if a registered strategy requires volume-profile inputs. The join must match exact symbol/date/time identity, treat study-local bar numbers as diagnostics, and route the combined feature contract only to that strategy; the current MA model must remain on its trained 13-feature contract.
8. Wire VP features into a deliberate new training contract rather than altering the deployed 13-feature MA model in place. After live preview/commit semantics are aligned, join each MA candidate to the freshest non-future historical VP snapshot, use the completed correlation report to compare full and reduced VP subsets with purged walk-forward validation, and create a separately versioned model only if VP improves out-of-sample results. Then implement the exact equivalent live feature join in the router for that registered model.
9. Audit the Zona labeling/export contract, preserve its actual event-end `t1`, regenerate its labeled datasets, and replace the duplicated fixed-row split functions in Zona trainers with imports from `training_mlp.validation`. MA is already migrated.
10. Build a separate CPCV evaluation runner once purged walk-forward produces a finalist. The runner must instantiate the existing `CombinatorialPurgedCrossValidation` module, fit preprocessing independently within every split, assemble the deterministic backtest paths, and report distributions of model and trading performance without modifying the untouched final holdout.
11. Tune and validate the router's 250 ms exact-feature timeout using measured live arrival timing.
12. Extend the new socket/stream integration coverage with Docker-backed Redis tests for actual consumer groups, pending-entry recovery, malformed stream entries, reconnects, and service restarts. The deterministic real-TCP/in-memory-Redis candidate-to-decision path is complete.
13. Perform a TradeStation soak test covering DLL reconnects, chart reloads, workspace reloads, market disconnects, and duplicate candidate submissions.
14. Define and enforce a unique, stable `instance_id` convention for every TradeStation strategy window. Candidate IDs are now GUIDs generated by the DLL.
15. Benchmark CPU versus GPU inference for the actual small MLP and choose the deployment device from measurements.
16. After the new route is proven live, remove or archive the legacy `inference_engine.py`, `trade_signal` configuration, and remaining historical setup paths so there is only one supported live inference path. Keep `consolidator.py` only if its manual debugging output remains useful or it becomes the basis for the production VP join.
17. After installing the staged hardened DLLs and recompiling the updated EasyLanguage documents, run a deliberate Python-outage test and confirm TradeStation remains responsive while ports 9009–9012 are unavailable and later reconnects cleanly.
